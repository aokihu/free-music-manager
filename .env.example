# Next.js 会在 npm run dev 时自动设置 NODE_ENV=development。
# 本地开发固定使用 storage/ 目录和 JSON 曲库，不需要额外变量。

# Cloudflare Workers 会在部署时通过 wrangler.jsonc 注入该值。
# 其他部署平台仍可通过运行环境设置同名变量。
# DEPLOYMENT_PROVIDER=cloudflare
